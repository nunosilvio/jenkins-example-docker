pipeline {
  agent {
    node {
      label 'ubuntu'
    }
  }
  stages {
    stage('Test') {
      agent {
        docker {
          image: 'node:16-alpine'
          args '-v $HOME/:/root/'
        }
      }
      steps {
        sh 'node --version'
        sh 'ls -la /root'
      }
    }
  }
}
